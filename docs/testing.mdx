---
title: "Testing"
description: "PLACEHOLDER tests, zero network calls, zero credentials"
---

```bash
pytest tests/ -v
```

Every test runs with no Alpaca keys, no Bright Data token, no Anthropic key,
and no network access. `tests/conftest.py` provides `FakeBroker` and
`FakeMarketData`; the news and LLM tests use `httpx.MockTransport` and a
fake Anthropic client respectively. CI runs the same suite with the process
environment stripped, as an extra check that nothing quietly depends on a
real credential being present.

## yfinance is offline by default, not by exception

An autouse fixture in `conftest.py` replaces the shared context provider
singleton with one that returns an empty payload and a stated gap:

```python
@pytest.fixture(autouse=True)
def _offline_market_context(monkeypatch):
    class OfflineProvider:
        def fetch(self, ticker: str) -> ctx.RawMarketData:
            return ctx.RawMarketData(gaps=["market context disabled in tests"])

    monkeypatch.setattr(ctx, "_provider", OfflineProvider())
```

Two things fall out of that. No test can reach yfinance even by accident —
and the shape it gets instead is exactly the shape a real outage produces, so
the **degraded path is the default in every test**, not a special case
someone has to remember to write. A test that wants real snapshots injects
its own provider. A sibling fixture redirects both log files to `tmp_path`,
so the suite never writes into the repository's `logs/` directory.

## Ambient credentials are removed, not hoped against

A second autouse fixture strips every credential that could be sitting on the
machine running the suite, and neutralises discovery of both CLIs' stored
logins:

```python
monkeypatch.delenv(news.CLI_ENV_VAR, raising=False)
monkeypatch.delenv("BRIGHTDATA_API_TOKEN", raising=False)
monkeypatch.setattr(news, "cli_credential_paths", list)

monkeypatch.delenv("ALPACA_API_KEY", raising=False)
monkeypatch.delenv("ALPACA_SECRET_KEY", raising=False)
monkeypatch.delenv(broker_client.CLI_PROFILE_ENV, raising=False)
monkeypatch.setenv(broker_client.CLI_CONFIG_DIR_ENV, "/nonexistent/alpaca-config")
```

Without it, a developer who had run `brightdata login` or `alpaca profile
login` would have a real credential on disk that the "missing credentials"
tests would quietly find — passing locally, failing in CI, or worse, reaching
the network. The Alpaca case is the one that matters most, since a real profile
could carry a live-trading bundle. Pointing `ALPACA_CONFIG_DIR` at a path that
cannot exist is what keeps the lookup off `~/.config/alpaca`; tests that
exercise the lookup point it at their own `tmp_path` instead. The same
reasoning applies to `ANTHROPIC_API_KEY` in `test_llm.py`, which clears it per
test. Tests that need the real path logic capture a reference at import time,
before the fixture replaces it.

## What each file proves

| File | Tests | Proves |
|---|---|---|
| `test_risk_engine.py` | 34 | The 5% cap holds pre- and post-rounding, existing exposure is counted, fractional shares are never returned, stop math is correct for both long and short, ATR-sanity rejects degenerate data. |
| `test_schemas.py` | 68 | `LLMSignal` rejects extra fields, enforces every bound (conviction range, ticker pattern, rationale length, the `[-1, 1]` score ranges, the six-item `key_factors` cap), and normalizes tickers to uppercase. |
| `test_market_data.py` | 6 | The Wilder ATR implementation matches a reference calculation, including gap and NaN handling. |
| `test_broker_client.py` | 26 | Alpaca credential resolution: settings before the CLI profile, an OAuth token before stored keys, half a key pair rejected rather than sent, every malformed-profile shape degrading, the credential never reaching the logs, and — proved to fail without the guard — that a profile marked `live_trade: true` is refused instead of borrowed. |
| `test_dispatch.py` | 13 | That direct mode is a transport change, not a safety change: mode selection and its refusal of a typo, the engine called in-process, no webhook secret read, both modes producing one journal shape, and order parameters still impossible. |
| `test_market_hours.py` | 11 | That a closed market costs nothing and trades nothing — the engine gate rejecting before any quote is fetched, the cycle gate skipping before any news or model call, an unreadable clock continuing rather than halting, and `--once` not starting the scheduler. Both gates proved load-bearing by removal. |
| `test_execution_engine.py` | 19 | The full nine-step decision path, every guardrail gate, and that broker/market-data errors become `ERROR` results rather than exceptions. |
| `test_order_idempotency.py` | 26 | That a retried submit cannot become a second position: the id is stable within a heartbeat window and distinct across ticker, side and window; a duplicate rejection is recognised while an ordinary failure is not; and a suppressed duplicate is reported `REJECTED`, never `ERROR`. Both halves were proved load-bearing by removal. |
| `test_webhook.py` | 12 | HTTP auth (401 on a bad secret), 422 on smuggled fields, that importing `app.main` needs no credentials, and correct handling of rejections. |
| `test_heartbeat.py` | 18 | The orchestrator can only ever send the closed signal shape; news failures skip the ticker while context gaps do not; missing credentials are logged per-ticker without killing the scheduler. |
| `test_news.py` | 49 | Bright Data request shape, response parsing (including the wrapped and fallback-key shapes), every `NewsFetchError` condition, and credential resolution: settings before the CLI's env var before its stored login, every credential-file shape tolerated, an unreadable file degrading rather than raising, and the token never reaching the logs. Also zone resolution: the SERP zone before the CLI's unlocker zone, the unlocker zone actually reaching the request body, and the missing-zone error naming both variables. |
| `test_llm.py` | 30 | Schema derivation can't drift from `LLMSignal` — including that `Optional[float]` is collapsed to a concrete type rather than pruned to an unconstrained `{}`, that `description` survives, and that every property comes back required. Also the request shape, every `LLMError` condition, the refusal fallback, and that a blank API key defers to `ant auth login` rather than failing at construction. |
| `test_technicals.py` | 16 | SMAs, Wilder RSI, MACD, trailing returns, the 52-week range, ATR% and volume ratio against reference values — and that too few bars yields `None` (rendered `n/a`) instead of raising. |
| `test_fundamentals.py` | 14 | Every carried field is parsed with the right unit, and the earnings date survives each of the shapes yfinance's calendar has been seen in. |
| `test_insiders.py` | 21 | That a stock grant, option exercise, gift or tax payment is never counted as insider buying; that sales are rendered as weak evidence; that distinct insiders are counted rather than transactions; and that no activity is distinguished from no data. |
| `test_analysts.py` | 10 | Ratings, the breakdown, targets and implied upside, upgrade/downgrade rows and holders — plus that a missing or empty frame degrades to an empty section rather than raising. |
| `test_context.py` | 18 | Each source fails independently into a named gap, `gather()` never raises, the slow-data cache honours its TTL, and the rendered prompt names what is missing. |
| `test_journal.py` | 5 | One JSON line per ticker per cycle carrying context, signal and outcome; cycles with no signal are recorded with `signal: null`; a write failure never propagates. |
| `test_analysis_returns.py` | 20 | That an entry price can never predate the signal that predicted it — swept across every entry rule, hour and day — plus after-hours handling, pending outcomes and missing history. |
| `test_analysis_metrics.py` | 24 | Rank correlation against hand-computed values including ties, that a constant series yields `None` rather than zero, signed-return direction, bucket edges, and the agreement and drift checks. |
| `test_analysis_reader.py` | 11 | Journal parsing field by field, the tz-naive timestamp fallback being flagged, and that a truncated final line is skipped rather than fatal. |
| `test_backtest_simulate.py` | 19 | The three ways a backtest flatters itself, each proved load-bearing by removal: ATR sized only from bars before the signal, stops hit on the intraday Low rather than the Close, and a gap through the stop filling at the open rather than the stop price. |
| `test_backtest_sweep.py` | 14 | That a tighter stop is hit more often and risks less per trade, that the cap changes size but not stop-hit rate, and that the report states its returns are not an edge and names what it does not model. |
| `test_replay_rebuild.py` | 17 | That a replayed prompt is byte-identical to the one the model was shown — including the nested `InsiderTrade` objects `asdict()` flattens, which was proved load-bearing by removal — plus schema drift tolerated in both directions and a context without a ticker refused rather than invented. |
| `test_replay_runner.py` | 24 | Loading (a truncated line skipped, a no-signal cycle still replayable), every model failure reported rather than raised, and change graded by consequence: a bias flip and a floor crossing count, a 0.05 conviction move does not. |
| `test_analysis_scoring.py` | 20 | The scorer end to end on a synthetic price series, plus report honesty: small samples flagged, NEUTRAL excluded, an all-pending journal still rendering. |

## A test suite is only as good as its negative cases

A recurring pattern in this codebase: when a test claims a guardrail holds,
there's a companion test proving the *opposite* input trips it. For example,
`test_risk_engine.py` doesn't just assert the 5% cap is respected on a
compliant input — it asserts that an input designed to overflow the cap
after rounding raises `RiskViolation`.

The same discipline was applied manually to the fallback logic while
building it: the switch-point-reading logic in `orchestrator/llm.py` was
temporarily reverted to prove `test_answer_is_taken_from_the_model_that_did_not_decline`
actually fails without it, before restoring the fix.

## Running a single file

```bash
pytest tests/test_risk_engine.py -v
```

## Running with coverage

```bash
pytest tests/ --cov=app --cov=orchestrator --cov=config
```

<Note>
  Coverage isn't wired into CI — the guardrail invariants job (see
  [Continuous Integration](/continuous-integration)) checks structural
  properties that line coverage can't, like "does anything outside
  `broker_client.py` import the Alpaca SDK."
</Note>
